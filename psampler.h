#ifndef PSAMPLER_H
#define PSAMPLER_H


class psampler
{
    public:
        psampler();
        virtual ~psampler();
        psampler(const psampler& other);
        psampler& operator=(const psampler& other);
    protected:
    private:
};

#endif // PSAMPLER_H
